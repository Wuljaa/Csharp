<Window x:Class="Filmovizija.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="350"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Background>
        <SolidColorBrush Color="#2C3E50" />
    </Window.Background>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Korisničko ime:" Grid.Row="0" Grid.Column="0"
                   Margin="5" VerticalAlignment="Center" Foreground="White" />

        <TextBox x:Name="UsernameTextBox" Grid.Row="0" Grid.Column="1"
                 Margin="5" Height="25" Width="200" />

        <TextBlock Text="Lozinka:" Grid.Row="1" Grid.Column="0"
                   Margin="5" VerticalAlignment="Center" Foreground="White" />

        <PasswordBox x:Name="PasswordTextBox" Grid.Row="1" Grid.Column="1"
                     Margin="5" Height="25" Width="200" />

        <Button Content="Login" Grid.Row="2" Grid.Column="1"
                Margin="0,10,10,10" Width="100" HorizontalAlignment="Right"
                Click="Login_Click" />
    </Grid>
</Window>
